import { AttractionNamePipe } from './attraction-name.pipe';

describe('AttractionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AttractionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
